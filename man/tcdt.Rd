% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/tcdt.R
\name{tcdt}
\alias{tcdt}
\title{Two-sample conditional distribution test}
\usage{
tcdt(
  X1,
  X2,
  Y1,
  Y2,
  x0 = NULL,
  B = 299,
  h = "cv",
  adj_bw = 0.1,
  C = 1,
  kern_smooth = c("gaussian", "epanechnikov", "uniform"),
  nu = 2,
  stat = c("ced", "cmmd"),
  kern_mmd = c("gaussian", "laplacian")
)
}
\arguments{
\item{X1, X2}{Matrices or data frames of the predictor variables for the two
samples. Each row is an observation, and columns are variables.}

\item{Y1, Y2}{Matrices or data frames of the response variables for the two
samples.}

\item{x0}{An optional numeric vector specifying the point for a local test.
If \code{NULL} (the default), a global test is performed.}

\item{B}{The number of bootstrap replications for calculating the p-value.
Default is 299. Set to 0 to compute only the test statistic.}

\item{h}{A character string or a function specifying the bandwidth selection
method for the smoothing kernel on \code{X}.
\itemize{
\item \code{"cv"}: (Default) Uses leave-one-out cross-validation.
\item \code{"undersmoothing"}: Uses a rule-of-thumb bandwidth that satisfies
the undersmoothing condition required by the theory.
\item A \code{function}: A user-provided function that takes \verb{(n, p, nu, C)}
and returns a bandwidth vector.
}}

\item{adj_bw}{A small numeric value used to adjust the exponent of the sample
size for the \code{"undersmoothing"} bandwidth to ensure the theoretical
undersmoothing condition is met. Default is 0.1.}

\item{C}{A numeric multiplier for the bandwidth selected by the
\code{"undersmoothing"} method or passed to a user-defined \code{h} function.}

\item{kern_smooth}{The type of smoothing kernel to use for the predictors \code{X}.
One of \code{"gaussian"}, \code{"epanechnikov"}, or \code{"uniform"}. Default is \code{"gaussian"}.}

\item{nu}{The order of the smoothing kernel. Default is 2.}

\item{stat}{The type of test statistic. One of \code{"cmmd"} (conditional maximum
mean discrepancy) or \code{"ced"} (conditional energy distance). Default is \code{"ced"}.}

\item{kern_mmd}{The type of RKHS kernel to use for the response \code{Y} when
\code{stat = "cmmd"}. One of \code{"gaussian"} or \code{"laplacian"}. Default is \code{"gaussian"}.}
}
\value{
A list containing the following components:
\item{problem}{A string indicating the type of test performed: \code{"global"} or \code{"local"}.}
\item{B}{The number of bootstrap replications used.}
\item{kern_smooth}{The smoothing kernel function used for \code{X}.}
\item{stat}{The type of test statistic used (\code{"ced"} or \code{"cmmd"}).}
\item{kern_mmd}{The RKHS kernel function used for \code{Y} (if \code{stat="cmmd"}).}
\item{h}{A matrix of the final bandwidths used for each sample and dimension.}
\item{Tn}{The calculated value of the test statistic.}
\item{h_b}{The bandwidths used for the bootstrap procedure (if \code{B > 0}).}
\item{Tn_b}{A vector of the bootstrap test statistics (if \code{B > 0}).}
\item{pvalue}{The computed p-value of the test (if \code{B > 0}).}
}
\description{
Performs a two-sample conditional distribution test proposed by Yan, Li and Zhang (2025),
using either conditional energy distance (CED) or conditional maximum mean discrepancy (CMMD).
The function can conduct both global tests for the equality of conditional
distributions over the entire support, and local tests at a specific point \code{x0}.
}
\details{
This function implements the testing framework proposed by Yan, Li and Zhang (2025).
When \code{x0} is \code{NULL}, a global test is performed using the integrated measure of
discrepancy between the conditional distributions.
When \code{x0} is specified, a local test is performed, focusing on the discrepancy
at that specific point.

Bandwidth selection for the smoothing kernel on \code{X} is crucial.
The default method \code{h = "cv"} uses leave-one-out cross-validation (\code{select_bw_cv})
to find an optimal bandwidth.

P-values are computed using a local bootstrap procedure which generates
bootstrap samples under the null hypothesis while preserving the marginal
distribution of \code{X}.
}
\references{
Yan, J., Li, Z., & Zhang, X. (2025). Distance and Kernel-Based
Measures for Global and Local Two-Sample Conditional Distribution Testing
arXiv preprint arXiv:2210.08149.
}
